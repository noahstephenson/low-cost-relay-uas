# Historical Cameo Model Report

> **Archive notice:** This is provenance material from a separate, older model baseline. It is not part of the current authoritative model, requirements set, traceability, feasibility analysis, or approval record.

The preserved [source HTML](cameo-model-report-2026-08-02.source.html) was created on August 2, 2026, from a Cameo/MagicDraw model identified inside the report as `Claude Test.mdzip`. The native report template did not produce a usable element report, so the document says its author assembled the content from live model queries.

## Disposition

- The file is retained unchanged in substance so the historical observation is not lost.
- Its subsystem-prefixed requirement identifiers belong to that older model and have no identity relationship to the current `REQ-001` through `REQ-023` catalog.
- Its block roster, approximate values, anomalies, and trace claims were not imported or accepted as current architecture truth.
- Current authority remains [model/system.yaml](../../model/system.yaml) and the catalogs it references.
- The source is registered as `SRC-REPO-009` with no current configuration applicability.

The archived source has SHA-256 `C38F99CBF4B5CA11BEDEC11D060D961F4C42E4ED8AC7875F605F3238F3AFB9F6`. Repository gap `GAP-CFG-001` is closed by this explicit historical classification, not by claiming the two models are equivalent.
