name: Check IDs

on:
  push:
  pull_request:

jobs:
  check-ids:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run ID consistency checks
        run: python scripts/check-ids.py
